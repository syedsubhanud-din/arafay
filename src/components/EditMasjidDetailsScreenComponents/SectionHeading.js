import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SectionHeading = () => {
  return (
    <View>
      <Text>Namaz Timings</Text>
    </View>
  );
};

export default SectionHeading;

const styles = StyleSheet.create({});
